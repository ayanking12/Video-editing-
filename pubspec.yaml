name: hdr_editor
description: "8K HDR Auto Video Editor"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  ffmpeg_kit_flutter_full_gpl: ^6.0.3
  file_picker: ^8.0.0
  path_provider: ^2.1.2
  gallery_saver: ^2.3.2

flutter:
  uses-material-design: true
